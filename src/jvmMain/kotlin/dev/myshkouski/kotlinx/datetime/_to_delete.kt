package dev.myshkouski.kotlinx.datetime

//internal fun LocalDateTime.toDateTime() = DateTime(year, monthValue, dayOfMonth, hour, minute, second)
//actual fun createDateTime(value: Long) = Instant.ofEpochMilli(value).atOffset(ZoneOffset.UTC).toLocalDateTime().toDateTime()
